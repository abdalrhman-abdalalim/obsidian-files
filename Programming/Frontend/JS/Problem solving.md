
### Create array with fixed size and fill it with value :- 
``` js
const arr = new Array (200);
arr.fill(0);
```
### Get ascii code of character :- 
``` js
let str = "abdo";
for(let i=0;i<str.length;i++){
	console.log(str.codeCharAt(i));
}
```

