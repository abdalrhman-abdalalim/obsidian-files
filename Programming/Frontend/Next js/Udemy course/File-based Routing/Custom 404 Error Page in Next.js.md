Next.js allows you to create a **custom 404 page** to display when users navigate to a route that doesn’t exist.

### 📁 File Location

Place the following file in your `pages/` directory:
```bash
/pages
  └── 404.js
```

